import React, { ReactElement } from 'react'

interface Props {
    
}

export default function About({}: Props): ReactElement {
    return (
        <div>
            About Page
        </div>
    )
}
