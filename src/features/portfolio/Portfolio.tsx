import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Portfolio({}: Props): ReactElement {
    return (
        <div>
            Portfolio Component
        </div>
    )
}
