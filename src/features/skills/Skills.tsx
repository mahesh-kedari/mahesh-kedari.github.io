import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Skills({}: Props): ReactElement {
    return (
        <div>
            Skills Page Here
        </div>
    )
}
