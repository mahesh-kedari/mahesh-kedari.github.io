import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Blogs({}: Props): ReactElement {
    return (
        <div>
            Blogs Component
        </div>
    )
}
